import { Template } from './template.types';import { FoodDetailTemplateV1 } from './foodDetail.v1';const TEMPLATE_REGISTRY: Record<string, Template> = {  food_detail_v1: FoodDetailTemplateV1};export function resolveTemplate(templateId: string): Template {  const template = TEMPLATE_REGISTRY[templateId];  if (!template) {    throw new Error(`Template not found: ${templateId}`);  }  return template;}
