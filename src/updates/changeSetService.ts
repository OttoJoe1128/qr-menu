import { randomUUID } from 'node:crypto';import { ChangeSet } from '../db';export function createChangeSet(patches: any[]): ChangeSet {  return {    id: randomUUID(),    status: 'draft',    patches,    createdAt: Date.now()  };}
