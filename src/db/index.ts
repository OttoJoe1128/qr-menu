import Dexie, { Table } from 'dexie';/* =====================   CORE (Hive: coreBox)   ===================== */export interface CoreState {  id: string; // single record: 'core'  schemaVersion: number; // db evolution  lastApprovedSnapshotId?: string;  updatedAt: number;}/* =====================   MENU (Hive: menuBox)   ===================== */export interface MenuItem {  id: string;  nameTR: string;  nameEN?: string;  templateId: string;  recipeId?: string;  tags: string[];  available: boolean;}export interface Recipe {  id: string;  ingredients: string[];  steps: string[];  pairings?: string[];}/* =====================   UPDATE SYSTEM   ===================== */export interface ChangeSet {  id: string;  status: 'draft' | 'review' | 'approved' | 'published';  patches: any[];  createdAt: number;}/* =====================   DATABASE   ===================== */class QRMenuDB extends Dexie {  core!: Table<CoreState, string>;  menuItems!: Table<MenuItem, string>;  recipes!: Table<Recipe, string>;  changeSets!: Table<ChangeSet, string>;  constructor() {    super('qr-menu-db');    this.version(1).stores({      core: 'id',      menuItems: 'id, nameTR, available',      recipes: 'id',      changeSets: 'id, status'    });  }}export const db = new QRMenuDB();
